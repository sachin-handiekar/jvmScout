# Run on 8081 so it does not collide with the collector on 8080.
server.port=8081
spring.application.name=error-demo

# Keep Spring's own startup/Whitelabel noise down; the agent logs captures itself.
logging.level.com.example.errdemo=INFO
server.error.include-message=always
